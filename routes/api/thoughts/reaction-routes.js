const router = require('express').Router();

// This POST route adds a new reaction to a user's thought list
router.post('/', (req, res) => {

});

//This DELETE route deletes a reaction from a user's thought, by ID.
router.delete('/:id', (req, res) => {

});
const router = require('express').Router();

// This POST route adds a new friend to a user's friend list
router.post('/', (req, res) => {

});

//This DELETE route deletes a friend from a user's friend list, by ID.
router.delete('/:id', (req, res) => {

});